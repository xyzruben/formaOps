# Ignore false positive malware advisories that are incorrectly flagging legitimate packages
# These advisories reference specific malicious versions that don't match our installed versions
audit-level=moderate
fund=false

# Package integrity and security
package-lock=true
save-exact=true